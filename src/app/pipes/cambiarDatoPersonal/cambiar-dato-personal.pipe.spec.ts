import { CambiarDatoPersonalPipe } from './cambiar-dato-personal.pipe';

describe('CambiarDatoPersonalPipe', () => {
  it('create an instance', () => {
    const pipe = new CambiarDatoPersonalPipe();
    expect(pipe).toBeTruthy();
  });
});
