import { PrimeraLetraMayusculaPipe } from './primera-letra-mayuscula.pipe';

describe('PrimeraLetraMayusculaPipe', () => {
  it('create an instance', () => {
    const pipe = new PrimeraLetraMayusculaPipe();
    expect(pipe).toBeTruthy();
  });
});
