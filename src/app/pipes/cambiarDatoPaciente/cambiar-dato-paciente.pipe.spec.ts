import { CambiarDatoPacientePipe } from './cambiar-dato-paciente.pipe';

describe('CambiarDatoPacientePipe', () => {
  it('create an instance', () => {
    const pipe = new CambiarDatoPacientePipe();
    expect(pipe).toBeTruthy();
  });
});
