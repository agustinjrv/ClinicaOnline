import { EscucharTecladoDirective } from './escuchar-teclado.directive';

describe('EscucharTecladoDirective', () => {
  it('should create an instance', () => {
    const directive = new EscucharTecladoDirective();
    expect(directive).toBeTruthy();
  });
});
