import { EnterDirective } from './enter.directive';

describe('EnterDirective', () => {
  it('should create an instance', () => {
    const directive = new EnterDirective();
    expect(directive).toBeTruthy();
  });
});
