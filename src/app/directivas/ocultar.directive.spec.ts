import { OcultarDirective } from './ocultar.directive';

describe('OcultarDirective', () => {
  it('should create an instance', () => {
    const directive = new OcultarDirective();
    expect(directive).toBeTruthy();
  });
});
