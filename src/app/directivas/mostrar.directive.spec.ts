import { MostrarDirective } from './mostrar.directive';

describe('MostrarDirective', () => {
  it('should create an instance', () => {
    const directive = new MostrarDirective();
    expect(directive).toBeTruthy();
  });
});
