import { NegativoDirective } from './negativo.directive';

describe('NegativoDirective', () => {
  it('should create an instance', () => {
    const directive = new NegativoDirective();
    expect(directive).toBeTruthy();
  });
});
